# FLOW-GLOSSARY-0.5 — Таблиця символів, сім'ї операторів, еррата

**Статус:** нормативний додаток до FLOW-0.1 та нотаток chat-0001. Створений як **акт амортизації** (RVB §5.4): кожен запис нижче закриває цілу родину мета-тверджень виду "X ніде не визначено", які інакше породжуються кожним актом верифікації заново (виміряно в EXP-RVB-1/1b).
**Правило пріоритету:** при конфлікті між цим файлом і текстом нотаток — діє цей файл; конфлікт фіксується як errata-кандидат, не замовчується.

---

## 1. Таблиця символів (глобальні прив'язки)

| Символ | Тип | Визначення | Статус |
|---|---|---|---|
| $X$ | множина | допустимі стани домену | Defined (FLOW §1) |
| $\mathcal{O}$ | множина | операції домену | Defined (FLOW §1) |
| $A(x)$ | функція $X \to 2^{\mathcal{O}}$ | **похідна**: $A(x) = \{a \mid C_i(x,a) \le 0\ \forall i\}$; не є незалежним каналом зміни (див. Еррату E1) | Defined (FLOW §5) |
| $C$ | предикати | constraints над $X \times \mathcal{O} \times T$ | Defined (FLOW §6) |
| $I$ | предикати | інваріанти, замкнені відносно допустимих переходів; завжди у формі "I під сім'єю F в межах B за припущень A" (FLOW §7) | Defined |
| $B_{bnd}$ | кортеж | boundary $(S_{in}, S_{out}, M, F)$; **лише** цей підпис. Голе "B" в інших ролях (Domain B, частинка B) — локальні імена, що НЕ успадковують §3 | Stipulated (Еррата E2) |
| $\tau$ | кортеж | temporal domain $(T, \prec, \lambda)$ | Defined (FLOW §10) |
| $\pi$ | функція | policy $a_t = \pi(x_t, h_t)$; інтервенція $do(\pi{=}\cdot)$ визначена відносно структурних рівнянь FLOW §19.1/§20 і є **regime-інтервенцією** (не точковою); для нерефлексивного player рівняння §20 вироджуються: $\pi_{t+1} = \pi_t$ | Stipulated |
| $T$ | функція | дискретний transition law $x_{t+1} = T(x_t, a_t, e_t)$ (FLOW §4, §19.1). **Перевантаження в FLOW** (носій часу в $\tau$ §10; слот ресурсного кортежа §8) — розводиться контекстом за E5 | Stipulated |
| $F_{tr}, G$ | функції | **(уточнено v0.5, G5):** $F_{tr}(x_t,\pi_t,e_t)$ — **композиція** $T(x_t, \pi_t(x_t,h_t), e_t)$, НЕ конкретизація $T$ (у $T$ аргумент — дія, у $F_{tr}$ — policy); $G$ — policy-update law зі змістом стипуляції: будь-яка функція $(\pi_t, x_t, Evidence_t) \mapsto \pi_{t+1}$, сама перезапускна над тими ж байтами Evidence (без цього G — ярлик, не стипуляція; закриває D14). Статуси рівнянь: §19.1 Stipulated; §20 — Stipulated **умовно** (умови — в рядку $Evidence_t$) | Stipulated |
| $e_t$ | змінна | екзогенне збурення у $T$/$F_{tr}$ (FLOW §4, §19.1, §20) | Stipulated |
| $Evidence_t$ | сорт | **(v0.5, закриває D8; v0.4-версію G5 НЕ зарахував і слушно)** Evidence = **reason-with-check**: пара (content-addressed байти + перевиконуване відношення до твердження). Референтне заземлення у Warrant — **`because[]`-запис виду {kind: check, check, runtime, verdict, transcript}**, НЕ голе поле `evidence` (SPEC §7: "mere presence in an evidence array forecloses nothing" — сам список хешів нічого не стверджує). Перезапускність **умовна**, і умови — частина стипуляції: (i) retention — блоби резолвляться в момент споживання $G$; (ii) runtime запінений (fingerprint runtime-індексований). Враження, переказ, "я перевірив" без байтів — не Evidence. Критерій "чи байти ПІДТРИМУЮТЬ твердження" лишається боргом D9 — D8 його не поглинає, лише межує | Stipulated (умовно; умови заявлені) |
| $\Phi_A$ | множина | обмеження-здатності агента $A$ (constraint family агента) | Stipulated |
| $Avail_A(x)$ | множина | $:= \{a \mid C_i(x,a) \le 0\ \forall C_i \in C \cup \Phi_A\}$ — опції ($\subseteq \mathcal{O}$), допустимі у стані $x$ під доменними обмеженнями **плюс** здатностями агента; при $\Phi_A = \emptyset$ збігається з $A(x)$ (FLOW §5) | Defined (відносно $\Phi_A$: Stipulated) |
| $Representable_A$ | множина | опції ($\subseteq \mathcal{O}$), які $A$ здатен репрезентувати; критерій репрезентації — Speculative до закриття E3, але сорт і роль зафіксовані | Stipulated |
| $PracticallyAvailable_A(x)$ | множина | $:= Avail_A(x) \cap Representable_A$; успадковує Speculative-епістеміку через $Representable_A$ | Stipulated |

## 2. Сім'я операторів Reach (закриває найбільшу родину дефектів)

`Reach` — **сімейно-індексований** оператор із фіксованим підписом $Reach^{m}_{\Phi}(S)$, де $m \in \{inf, caus\}$ — модальність, $\Phi$ — сім'я обмежень (літера $\Phi$ обрана замість $F$, щоб не колідувати з transition law та фільтром $B_{bnd}$ — див. E5), $S$ — база. Голий запис `Reach(S)` нелегальний у нових текстах; легальні форми:

- $Reach^{inf}_\Phi(S)$ — **інференційний**: $S$ = warranted-регіон тверджень, результат — множина тверджень, легітимно вивідних (сенс файлів 0002–0005).
- $Reach^{caus}_\Phi(S)$ — **каузально-агентивний**: $S \subseteq X$ = множина станів, результат — **стани** ($\subseteq X$), досяжні допустимими переходами за обмежень $\Phi$ (сенс файлів 0014, 0025, 0042, 0072).
- **Агент входить через $\Phi$, не через окремий слот:** скорочення $Reach^{caus}_A$ означає $Reach^{caus}_{\Phi_A}(\{x_{now}\})$. (Закриває D2/D7.)
- **$\Phi$ — сортово-індексована сім'я, заявлено явно (v0.5, закриває D13):** $\Phi^{caus}$ — предикати над $X \times \mathcal{O} \times T$ (C-стиль); $\Phi^{inf}$ — ліцензії виводу над твердженнями. Один гліф, дві координати сорту, індексовані тією ж $m$, що й Reach — це оголошений поліморфізм, не колізія (колізія — коли ролі не заявлені; E5-урок).
- **Сорти:** $Reach$ — про стани; про опції — $Avail_A(x)$ з таблиці §1. Плутати їх — type error. (Закриває D1.)
- Часткові індекси з 0019 (physical / predictive / experiential / interpretive / emotional) — підсім'ї $Reach^{caus}$ або $Reach^{inf}$; кожне вживання декларує, якої.

**Наслідок для 0072 §299 (ремонт v0.2, закриває D3).** Оригінал $RepresentableReach \subseteq CausallyPossibleReach$ у загальному вигляді **хибний** (уява за 0004/0005 — це $Generate(D), D \notin Reach$). Ремонт розщеплює його на дві чесно типізовані частини:
1. **Дефініційна (Stipulated):** $PracticallyAvailable_A(x) := Avail_A(x) \cap Representable_A$ — тавтологічна за побудовою, і це нормально: вона фіксує словник.
2. **Емпірична (Speculative до свідка):** $\exists A, x: Avail_A(x) \setminus Representable_A \neq \emptyset$ — існують допустимі, але нерепрезентовані опції; саме це і означає "брак поняття звужує практичну досяжність". Свідок робить її Diamond⁺ (0025).

## 3. Статуси визначень (типізація тверджень корпусу)

Кожен термін корпусу має рівно один статус; за замовчуванням — найслабший:

- **Defined** — є формальне визначення з доменом/кодоменом.
- **Stipulated** — зафіксовано рішенням у цьому файлі; можна оскаржити, не можна мовчки переозначити.
- **Borrowed-name** — позикове ім'я без онтологічних зобов'язань (FLOW §29: ATP, Warrant у прикладах; вся гідродинаміка VORTEX).
- **Speculative** — не має визначення; твердження, що на нього спираються, автоматично типу Speculative. Стрілка `→` в нотатках chat-0001 (54+ вживань без семантики) — **Speculative-конектор**: ескіз каузального/темпорального зв'язку, не оператор. Формули з нею — діаграми інтуїції, не claims.

**Правило статусу для формул-рівнянь (v0.5, узгоджено після G5):** рівняння не є термом і не отримує статусу за замовчуванням; його статус = **найслабший статус серед його термів**. **Умовна стипуляція легальна:** Stipulated-статус може нести заявлені умови (retention, runtime тощо); умови — частина стипуляції, і статус успадковується як умовний (закриває "відсутню клітинку" з G5). Застосування: §19.1 → Stipulated; §20 → Stipulated *умовно* (через умови $Evidence_t$). Якір $do(\pi{=}\cdot)$: для нерефлексивного player — Stipulated; для рефлексивного — Stipulated умовно, **але з відкритим концептуальним боргом D15**: regime-інтервенція $do(\pi{=}\cdot)$ перезаписує $\pi$-рівняння і тим самим **вимикає $G$** — контрфакт, що сертифікує рефлексивного player, обчислюється на системі, яка вже не рефлексивна. Легальність релятума ≠ що якір тестує рефлексивність; чесний тест рефлексивності потребує інтервенції на рівні $G$, не $\pi$ (кандидат FLOW-0.2).

## 4. Еррата

- **E1 (FLOW §15).** Фраза "через зміну $A(x)$, $C$ або $I$" читається як "через зміну $C$ або $I$": $A(x)$ похідна від $x$ і $C$ (§5) і не є третім каналом. Поділ "фізична/інформаційна зміна" визначений **відносно заявленого $B_{bnd}$**: інформаційна зміна — зміна $C$ або $I$ без зміни станів усередині $S_{in}$; без заявленого boundary поділ не визначений. (Закриває дефект V1.)
- **E2 (FLOW, весь текст).** Символ B перевантажений трьома ролями (boundary §1/§3; Domain B у Appendix A; частинка B у §32/Appendix B). Норма: boundary — тільки $B_{bnd}$; інші вживання — локальні імена своїх секцій. Рядок Appendix B "Boundary $B$ між $A$ і $B$" читається "$B_{bnd}$ між частинками $A$ і $B$".
- **E3 (FLOW §17).** "Representational capacity" має статус Speculative до появи операційного критерію; визначення Player діє в ослабленій формі (ендогенний контроль + контрфактичний вплив policy), яка свідомо не відрізняє player від складного reactive system. Це чесна межа, не баг.
- **E4 (корпус, шаблон Burden).** Формули виду $Burden \propto X \times Y \times Z$ (0043, 0063, 0068, 0072) мають статус Speculative: множники несумірні, коефіцієнтів немає. Легальне читання — "зростає за кожним з аргументів" (монотонність), не добуток.
- **E5 (FLOW + глосарій v0.2, знайдено G4).** Літери $F$ і $T$ перевантажені: $F$ = континуальний flow law (§4) / transition law з policy (§20) / фільтр у $B_{bnd}$ (§3); $T$ = дискретний transition law (§4, §19.1) / носій часу (§10) / слот ресурсного кортежа (§8). Норма v0.3: transition law з policy пишеться $F_{tr}$; індекс сімей Reach — $\Phi$; фільтр лишається четвертим слотом $B_{bnd}$ і поза ним не вживається; $T$ поза §10/§8 = transition law. Виправлено також міс-цитату v0.2: $F_{tr}$ конкретизує $T$ з §4, а не $F(x,t)$.

## 5. Реєстр боргів глосарія (за 0040: obligation = memory with an open edge)

Глосарій — теж твердження; його аудит (G1–G3, 2026-08-30) знайшов власні дефекти. Реєстр версіонований; кожна версія зобов'язана або закрити борг, або перенести його явно.

| Борг | Зміст | Статус |
|---|---|---|
| D1 | type error: $Reach^{caus}$ над станами, "опції" — елементи $\mathcal{O}$ | **ЗАКРИТО v0.2** (підтверджено G4: "the sort split is real") |
| D2 | індекс $Reach^{caus}_A$: який слот займає $A$ | v0.2 закрито → **G4 ПЕРЕВІДКРИВ** (символ F ніс 3 ролі) → **ЗАКРИТО v0.3**: індекс перейменовано на $\Phi$, E5 розводить $F$/$T$ |
| D3 | $PracticallyAvailable_A$ без визначення | **ЗАКРИТО v0.2/v0.3**; G4 зауваження про нелегальний статус виправлено (тепер Stipulated); критерій свідка для емпіричної частини — новий борг D9 |
| D4 | нема правила статусу для формул-рівнянь | v0.2 закрито → **G4 ПЕРЕВІДКРИВ** ($T, e_t, Evidence_t$ поза таблицею) → **ЗАКРИТО v0.3 частково**: §19.1 Stipulated (якір для нерефлексивного player), §20 чесно Speculative через $Evidence_t$ → D8 |
| D5 | FLOW: carrier-відношення constraint↔$B_{bnd}$ відсутнє | **ВІДКРИТО** (борг FLOW-0.2) |
| D6 | нема процедури ремонту власних дефектів | **ЗАКРИТО**: цей реєстр |
| D7 | (G4) потрійна роль $F$, потрійна роль $T$ | **ЗАКРИТО v0.3**: E5 + $F_{tr}$ + $\Phi$ |
| D8 | (G4) $Evidence_t$ без визначення → рефлексивна гілка §20 Speculative | v0.4 закриття **НЕ ЗАРАХОВАНО G5** (внутрішня суперечність із §3; заземлення в поле `evidence` замість `because[].check` — конфляція схеми Warrant, стверджена з пам'яті без перечитання SPEC = порушення OAH самим редактором) → **ЗАКРИТО v0.5**: reason-with-check, умовна стипуляція з заявленими retention/runtime |
| D9 | (G4) критерій прийняття свідка для репрезентовності | **ВІДКРИТО** (E3); G5 підтвердив: D8 межує з D9, не поглинає його |
| D10 | (G5) внутрішня суперечність v0.4: рядок Evidence vs §3 і рядок F_tr | **ЗАКРИТО v0.5**: нормативні пасажі узгоджені |
| D11 | (G5) конфляція `evidence`/`because` у заземленні | **ЗАКРИТО v0.5**: перезаземлено в `because[].check` |
| D12 | (G5) $F_{tr}$ — композиція $T$, не конкретизація | **ЗАКРИТО v0.5**: описано як композицію |
| D13 | (G5) $\Phi$ сортово-поліморфна між $m$ без оголошення | **ЗАКРИТО v0.5**: поліморфізм оголошено явно (§2) |
| D14 | (G5) $G$ — Stipulated ярликом без змісту | **ЗАКРИТО v0.5**: стипуляції надано зміст |
| D15 | (G5, глибокий) $do(\pi{=}\cdot)$ вимикає $G$ → сертифікація рефлексивності виконується на нерефлексивній системі | **ВІДКРИТО** (кандидат FLOW-0.2: інтервенція на рівні $G$) |

**Дисципліна кредиту:** закриття v0.5 — **НЕВЕРИФІКОВАНІ** (G6 не запускався; бюджет петлі цієї сесії вичерпано свідомо). **Крива боргу за п'ять ітерацій: 6 → 3 → 3 → 7 → 3 відкритих (D5, D9, D15) + неверифіковані v0.5.** Урок четвертої точки, виміряний: стискання **немонотонне** — неохайний ремонт (доданий рядок без узгодження норм, незвірене заземлення) карбує борг швидше, ніж гасить. Петля карає поспіх і винагороджує перечитування джерел.

## 5a. Виміряні пріоритетні розрізнення (з TERM-GRAPH-0.1 §3)

Записи впорядковані за виміряною окупністю (скільки разів корпус платив за розведення повторно). Кожен рядок — одна типізована відмінність; файли-джерела дають повні декомпозиції.

- **Blame ≠ Punishment** (5×): blame — атрибуція нормативної відповідальності за минулий акт; punishment — накладена ціна. Кожне буває без іншого: blame без покарання (визнання), покарання без blame (страховий штраф, collective punishment).
- **Authentication ≠ Authorization** (4×): встановлення "хто це" ≠ рішення "що йому можна". Помилка змішування — класична дірка безпеки.
- **Authority ≠ Competence** (4×): право вирішувати ≠ здатність вирішувати добре. Змішування породжує і карго-культ експертизи, і делегітимацію компетентних.
- **Blame ≠ Repair** (4×): пошук винного ≠ відновлення пошкодженого; системи, що оптимізують перше, систематично недоінвестовують друге (0065).
- **Control ≠ Stewardship** (4×): влада над об'єктом ≠ обов'язок дбати про нього в чужих інтересах; steward, що робить себе незамінним, порушує роль (0047).
- **Desert ≠ Fairness** (4×): "заслужив" ≠ "справедливо розподілено"; merit — один з багатьох принципів алокації, не синонім справедливості (0068).
- **Fairness ≠ Need** (4×): справедливість процедури ≠ задоволення потреби; конфліктують у тріажі (0068).
- **Punishment ≠ Repair** (4×): див. Blame≠Repair; кара не відновлює.
- **Argument ≠ {Coercion, Framing, Manipulation, Nudge}** (3× кожне): аргумент адресує здатність адресата оцінювати підстави; решта — обходять її різними каналами (0057).

## 6. Translation report цього файлу

- **Preserved:** зміст усіх визначень FLOW §1–§14; обидва сенси Reach; наміри 0072 §299.
- **Lost:** нічого не видалено; частину тверджень **понижено в типі** (Speculative), що звужує їхній forward cone — це і є мета.
- **Introduced:** стипуляції розділів 1–3 (кожна — оскаржувана, але явна).
