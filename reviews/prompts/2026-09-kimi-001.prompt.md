# Review request — s0fractal/manifesto `papers/` (out-of-lineage adversarial review)

You are reviewing two research write-ups in a public repository before an **intermediate**
Zenodo deposit. The deposit is a dated trajectory marker, not a claim of validation: there are no
human reviewers on this project; all prior review is by AI models (Codex/OpenAI, Qwen, Claude).
You are the first reviewer from outside both the Anthropic lineage that *wrote* the papers and the
OpenAI lineage that blocked v0.1. That is why you are here — please do not spend your effort
repeating what those reviews already found.

Repository: https://github.com/s0fractal/manifesto — review at commit `<25123df>` (bind every
finding to a path and, where possible, a line or content digest at that commit).
Your review will be filed **as received**, unedited, as `reviews/2026-09-kimi-001.md`, with any
response in a separate file. Please state which model and version you are, and the date.

## Step 0 — Resurrection probe (do this BEFORE opening `papers/`)

Read only the root `README.md` and `papers/README.md`. In at most five sentences, state what the
paper `every-check-spawns-more` establishes, as you understand it from those two files alone.
Record this verbatim at the top of your review under the heading **"Step 0 — prior from README"**.
Do not revise it later. (This tests whether the repository's front matter still transmits claims
the papers have retired.)

## Already covered — do not repeat

The following are known and ledgered (`papers/*/CLAIM-LEDGER.md`, `MIGRATION-NOTES.md`,
`DEPOSIT-AND-AUDIT.md`, `reviews/2026-09-codex-papers-v0.2.md`). Cite them if relevant; do not
re-derive them:

- the v0.1 branching/queue "theorem" is retired; closed forms (C) and (D) have scheduler counterexamples;
- the frozen act corpus (prompts, offspring texts, dedup/removal decisions) is not yet in the repo;
- `check_claims.py` confirms the μ/ô tables by string presence, not recount;
- all verifiers are one vendor's LLMs; ô is self-adjudicated by the verifier;
- paper B's 601-ATP figure is from a permissive harness; `church@v0` is a DRAFT that refuses `PLUS 7 5`;
- the abstract of v0.2 is over-hedged; placeholders and missing `[@key]` citations remain.

General impressions, praise, and philosophical commentary on the manifesto are out of scope.

## Three targeted tasks

### Task 1 — Does ô measure the claim or the verifier? (paper A, §3, §5)

Using the act records available (`drafts/EXP-RVB-1-RESULTS.md` and, if present, the exported act
corpus), do two things:

1. For the four **crossed roots** (§5), compute the overlap between the offspring sets produced by
   the two verifiers on the same root (Jaccard on your own normalization, plus a judged semantic
   overlap). Report the numbers and your method. Interpret: if overlap is low, ô is a property of
   the verifier's generative distribution, not of the claim's obligations — say so plainly and
   say what the abstract must then say next to the number.
2. Sample offspring at depths 3–4 from round 2 and judge, item by item, how many restate an
   assertion already recorded earlier in the same chain (paraphrase leakage past LLM dedup).
   Give the fraction. State whether the reported plateau (ô ≈ 2.1–2.25 at depths 3–4) survives.

If the data needed is not in the repository, say exactly which fields are missing, and do the
part that is possible.

### Task 2 — Is §7 actually an open problem? (paper A, §7)

Under the explicitly stated assumptions — work-conserving FIFO service, no abandonment,
each root's closure a Galton–Watson tree with offspring mean μ, arrivals at rate λ_G, acts at
rate λ_V — is the stability/throughput statement a known result in queueing with branching or
cascading service (e.g. Crump–Mode–Jagers / Bellman–Harris lines, queues with branching-type
arrivals)? Provide either a citation with the matching statement, a short derivation, or a
counterexample under those exact assumptions. Also: fix the `g` inconsistency the migration notes
flag (closure fraction vs λ_G/λ_V) by proposing the minimal well-posed model the paper should
state, and list what it must fix that the current text does not.

### Task 3 — Prior-art search log for paper B (`addressing-is-equality`)

Paper B's novelty is ledgered as OPEN (F1). Produce the search log the deposit needs. For each of
the following, state whether it anticipates the claimed sliver — *equality settled as a
budget-priced computation whose receipt is the pair of normal-form addresses plus the spend* — or
only a component of it:

- normalization by evaluation, specifically the **freshness** side condition on residualized
  variables (is the marker-collision counterexample in §3.1 just this condition rediscovered?);
- Unison (content-addressed definitions; hash as identity of code);
- Dhall semantic integrity hashes (hash of the normal form as the import check);
- Nix derivation hashes; IPLD; hash-consing (Ershov; Filliâtre–Conchon); Merkle identity;
- any metered/priced evaluation with receipts you know of (gas-metered VMs count).

Conclude with a verdict on F1: does the note reduce to a citation, or does a sliver survive, and
if so, state it in one sentence narrower than the paper's.

### Task 4 (short) — Reproduction

In a clean environment: `pip install sigma-glyph==0.6.7`, run
`papers/every-check-spawns-more/check_claims.py` and `tools/conf_mono_settle.py`. Report which
checks re-derive a number and which only test string presence, in a two-column list.

## Output format

Markdown. Sections in this order: Step 0 verbatim; Disposition (one of: deposit-ready / repairable /
blocked, with one sentence why); Task 1–4 findings. Each finding typed using the repository's own
vocabulary — `integrity_break`, `within-sample_counterexample`, `replication_failure`,
`scope_boundary`, `competitive_result`, `misuse_warning`, `open_obligation` — and bound to a path
at `<COMMIT>`. A finding that kills a claim is a contribution; the ledgers say what each kill
demotes. Be specific and short; numbers over adjectives.
